Backend source of truth

This folder mirrors deployed server code for Decoy Wallet.
No secrets are stored in the repo.
Each service includes env.example and deployment notes.
We commit exact deployed code first, then make changes.
